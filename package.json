{
  "name": "tabattack",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "http-server . -p 8080 -c-1",
    "dev": "concurrently \"npm run watch\" \"npm run serve\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "concurrently": "^8.2.2",
    "http-server": "^14.1.1",
    "nodemon": "^3.0.2",
    "typescript": "^5.0.0"
  }
}