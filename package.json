{
  "name": "tabattack",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "dev": "nodemon --watch src --ext ts --exec \"npm run build\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "nodemon": "^3.0.2",
    "typescript": "^5.0.0"
  }
}