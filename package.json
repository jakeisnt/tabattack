{
  "name": "tabattack",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "dev": "nodemon --watch src --ext ts --exec \"npm run build\"",
    "serve": "http-server . -p 8080 -c-1",
    "start": "npm run serve"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "http-server": "^14.1.1",
    "nodemon": "^3.0.2",
    "typescript": "^5.0.0"
  }
}